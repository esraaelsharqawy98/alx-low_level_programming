#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: integer number as parameter
 * @b: integer number as parameter
 * Return: return the sum of a, b.
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
